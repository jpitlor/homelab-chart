apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "homelab-containers.fullname" . }}-test-connection"
  labels:
    {{- include "homelab-containers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "homelab-containers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
