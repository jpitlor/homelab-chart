apiVersion: v1
kind: Service
metadata:
  name: {{ include "homelab-containers.fullname" . }}
  labels:
    {{- include "homelab-containers.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homelab-containers.selectorLabels" . | nindent 4 }}
